import Shape from './shape';

/*
class Triangle extends Shape {
  public get type(): string {
    return 'triangle';
  }

  public draw(): void {
    this.polygonShape(3);
    this.applyStyle();
  }
}

export default Triangle;
*/
